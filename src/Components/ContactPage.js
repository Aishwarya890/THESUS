import React from 'react';

const Contact = () => {
  return (
    <div
      style={{
        width: '100%',
        height: '300px',
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <h1 style={{ textAlign: 'center' }}>Contact</h1>
      <br />
      <h2 style={{ textAlign: 'center' }}>
        Contact us at support@thesusoutdoors.com.
      </h2>
    </div>
  );
};

export default Contact;
